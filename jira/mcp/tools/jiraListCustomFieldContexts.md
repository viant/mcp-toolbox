List contexts for a Jira custom field.

Inputs:
- account.alias (optional): account alias (default: "default").
- fieldId (required): Jira custom field id (e.g. "customfield_10571").

Output:
- contexts: list of {id, name}.
