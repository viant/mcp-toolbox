Add a comment to a Jira issue.

Inputs:
- account.alias: optional alias (defaults to "default").
- key: required issue key (e.g. ABC-123).
- body: required comment text.

Output:
- id: comment identifier.

