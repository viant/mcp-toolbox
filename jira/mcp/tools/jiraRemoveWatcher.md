Remove a watcher from a Jira issue.

Inputs:
- account.alias: optional alias (defaults to "default").
- key: required issue key.
- accountId: required accountId to remove.

Output:
- removed: true on success.
