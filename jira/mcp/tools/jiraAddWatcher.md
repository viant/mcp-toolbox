Add a watcher to a Jira issue.

Inputs:
- account.alias: optional alias (defaults to "default").
- key: required issue key.
- accountId: required accountId to add as watcher.

Output:
- added: true on success.
