List Jira projects for the specified account alias. Use when you need to discover project keys and names.

Inputs:
- account.alias: optional account alias (defaults to "default").

Output:
- projects: array of { id, key, name }.

