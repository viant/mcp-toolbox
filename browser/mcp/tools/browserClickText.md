# browserClickText

Convenience tool for clicking a visible element by text (maps to `browserClick` with `locator.text`).

Input:
```json
{
  "sessionID": "localhost:4444",
  "text": "Chat",
  "exact": true,
  "timeoutMs": 10000
}
```

