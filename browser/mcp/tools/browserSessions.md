# browserSessions (alias: webdriverSessions)

Lists known browser sessions.

By default returns only currently open sessions; set `includeAll=true` to include sessions that exist but are not open.

## Input
- `includeAll`: bool

