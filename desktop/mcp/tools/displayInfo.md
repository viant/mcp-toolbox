# `displayInfo`

Returns display-related information and current selection.

Output includes:
- `displayId` (current RobotGo `DisplayID`)
- `mainId`
- `numDisplays`
- `scale` (SysScale for selected display)
- `rects` (best-effort rect per display id)

