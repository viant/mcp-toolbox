# `convertCoords`

Converts between logical (points) and physical (pixels) coordinates using the display scale.

Input:
```json
{ "mode": "logicalToPhysical", "x": 10, "y": 10, "w": 100, "h": 50, "displayId": -1 }
```

Modes:
- `logicalToPhysical`
- `physicalToLogical`

