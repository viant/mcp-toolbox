# `clickText` (OCR, optional)

Finds text on screen via OCR and clicks the first match (center of the bounding box).

Input:
```json
{ "text": "OK", "rect": { "x": 0, "y": 0, "w": 800, "h": 600 }, "lang": "eng", "button": "left", "double": false }
```

