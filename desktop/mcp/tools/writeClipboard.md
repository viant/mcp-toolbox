# `writeClipboard`

Writes `text/plain`, `text/html`, or `image/png` to the system clipboard (best-effort; full support is macOS-only today).

Input:
```json
{ "mimeType": "text/plain", "text": "hello" }
```
