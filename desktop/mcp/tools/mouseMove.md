# `mouseMove`

Moves the mouse.

Input:
```json
{ "x": 100, "y": 200, "smooth": true, "mouseSleepMs": 0 }
```

Notes:
- Coordinates are screen coordinates as used by `robotgo` (may be affected by OS scaling).
