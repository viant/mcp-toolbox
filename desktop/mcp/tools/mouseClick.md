# `mouseClick`

Clicks the mouse.

Input:
```json
{ "button": "left", "double": false, "mouseSleepMs": 0 }
```

Optional:
- `x`,`y`: if set, moves before clicking.
- `down`: `"down"` or `"up"` to press/release without a full click.
