# `keyTap`

Taps a key with optional modifiers.

Input:
```json
{ "key": "v", "modifiers": ["cmd"], "keySleepMs": 10 }
```

Notes:
- Key names follow RobotGo conventions (see RobotGo docs for the full list).
