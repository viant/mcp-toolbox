# `setDisplay`

Sets the current display selection used by screen capture operations.

Input:
```json
{ "displayId": 0 }
```

Notes:
- Sets the `desktop-mcp` server default display (it does not mutate RobotGo global `DisplayID`).
